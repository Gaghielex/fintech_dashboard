// Country flags use 2-char ISO codes (lowercase) — FlagBadge resolves these to
// flagcdn.com images. Non-country entries use emoji strings as fallback.
export const GEO_FLAGS = {
  AU: 'au',
  JP: 'jp',
  EC: 'ec',
  retirement: '🏦',
}
